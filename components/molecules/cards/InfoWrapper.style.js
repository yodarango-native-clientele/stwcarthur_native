import {StyleSheet} from 'react-native';

export default EventCard = StyleSheet.create({
  infoWrapper: {
    height: '60%',
    backgroundColor: '#242424',
    padding: 5,
    opacity: 0.8,
  },
});
